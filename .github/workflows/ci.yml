name: CI

on:
  push:

jobs:
  failing-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Print test message
        run: echo "Testing DeployFix AI"

      - name: Force failure
        run: exit 1